export default function MainContent(){
    return(
        <main>
        <input type="search" id="searchbox" placeholder="e.g. pepper" />
        <button>+ Add Ingredient</button>
        </main>
    )
}